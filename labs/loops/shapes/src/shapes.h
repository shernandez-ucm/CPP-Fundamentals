#pragma once
#include <sstream>

// Function prototypes for shapes lab
std::stringstream get_triangle(int height);
std::stringstream get_flipped_triangle(int height);
void print_shape(const std::string);
std::stringstream get_square(int height);
void clear_scren();